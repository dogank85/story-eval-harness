{
  "name": "story-eval-harness",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "description": "Provider-agnostic evaluation harness for AI-generated children's stories: catches readability, schema, and other output-quality regressions before a prompt or model change ships.",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./run": "./src/run.ts"
  },
  "scripts": {
    "eval": "tsx src/run.ts",
    "eval:regression": "tsx src/regression-demo.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.4.5"
  }
}
